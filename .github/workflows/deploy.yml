# Continuous deployment for the multi-wallet PLAYTEST demo (Railway project sdp-mw-playtest).
# Railway's own push-to-deploy was not firing, so we drive it from Actions: every push to
# feat/multi-wallet runs `railway up` against the dashboard service. The build uses
# railway.json -> Dockerfile.railway (runtime env-config entrypoint). RAILWAY_TOKEN is a
# project-scoped token (repo secret), so the CLI needs no `railway link`.
name: Deploy dashboard to Railway (playtest)

on:
  push:
    branches: [feat/multi-wallet]
  workflow_dispatch: {}

concurrency:
  group: railway-deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Railway CLI
        run: npm install -g @railway/cli
      - name: Deploy
        run: railway up --ci --service dashboard
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
